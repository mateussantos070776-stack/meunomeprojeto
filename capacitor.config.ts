import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.meunomeprojeto.biblia',
  appName: 'Central de Estudos Biblicos',
  webDir: 'meunomeprojeto'
};

export default config;
